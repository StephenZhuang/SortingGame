# SortingGame Phase 2-4 实现计划（体验完善 + 排行榜 + 打磨）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Phase 1 MVP 基础上完成 Phase 2（键盘快捷键、标记系统、公布答案/结算、设置）、Phase 3（本地排行榜）、Phase 4（音效、动画、iOS target 接入、三端验证、App Store 准备）。

**Architecture:** 延续「纯逻辑核心 + 渲染/输入适配」：选中/标记等游戏状态全部收进 GameCore（GameEngine），GameScene 变为纯渲染器（每帧 diff 引擎状态同步视觉），SwiftUI 通过 @Observable 订阅引擎状态。键盘/触摸输入都翻译成 `engine.tap(position:)` 一个语义入口。新增 Services 层（SettingsService、LeaderboardService、SoundEffectPlayer）。

**Tech Stack:** Swift 6（严格并发）、SwiftUI、SpriteKit、XCTest、UserDefaults（设置）、JSON 文件（排行榜）

---

## 前提：Phase 1 遗留 bug（在本计划 Task 1 / Task 3 / Task 4 中修复）

1. `GameEngine.startNewGame()` 中 `currentArray = targetArray`——玩家开局即持正确答案，首次提交即通关。正确设计：目标排列随机生成且保证 ≠ 自然序；玩家从自然序 `[0,1,2,...]` 开始通过交换推理还原。
2. `GameScene.setupGame()` 中瓶子节点加到 scene 上，但坐标用的是 `boxNode.getSlotPosition`（boxNode 局部坐标），渲染位置偏移。修复：瓶子作为 boxNode 的子节点。
3. `GameView.makeScene()` 在 body 中调用，视图刷新会重建场景丢失交互状态。修复：scene 存入 `@State` 只创建一次。

---

## 验证命令（全局约定）

- 编译 macOS：`xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
- 编译 iOS：`xcodebuild -scheme SortingGame-iOS -destination 'generic/platform=iOS Simulator' build`
- 全部测试：`xcodebuild -scheme SortingGame -destination 'platform=macOS' test`
- 单个测试类：`xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/<类名>`

工程使用 Xcode 16 文件系统同步 group：在 `SortingGame/`、`SortingGameTests/` 目录下新建文件会自动加入对应 target，无需改 pbxproj（iOS target 接入除外，见 Task 13）。

---

# Phase 2：完善体验

## Task 1: 修复 GameEngine 初始排列 bug

**Files:**
- Modify: `SortingGame/GameCore/Engine/GameEngine.swift`
- Test: `SortingGameTests/GameCore/GameEngineTests.swift`

- [x] **Step 1: 写失败测试 + 修正受影响的旧测试**

在 `GameEngineTests.swift` 中新增两个测试，并**替换**原来的 `testSubmitIncorrectAnswer`（旧实现依赖随机排列打乱，修复后自然序保证 ≠ 目标，直接提交必然错误，测试更确定）：

```swift
func testStartNewGameCurrentArrayIsNaturalOrder() {
    var engine = GameEngine(bottleCount: 4)
    engine.startNewGame()
    XCTAssertEqual(engine.currentArray?.bottles.map(\.id), [0, 1, 2, 3])
}

func testStartNewGameTargetDiffersFromCurrent() {
    var engine = GameEngine(bottleCount: 4)
    // 随机生成多次，目标必须始终 != 自然序
    for _ in 0..<50 {
        engine.startNewGame()
        XCTAssertNotEqual(engine.currentArray, engine.targetArray)
    }
}

func testSubmitIncorrectAnswer() {
    var engine = GameEngine(bottleCount: 4)
    engine.startNewGame()

    // 自然序必然 != 目标排列，直接提交必然错误
    engine.submitGuess()

    XCTAssertEqual(engine.state, .playing)
    XCTAssertEqual(engine.scoreCalculator.attemptCount, 1)
}
```

- [x] **Step 2: 运行测试验证失败**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/GameEngineTests`
预期：`testStartNewGameCurrentArrayIsNaturalOrder` 与 `testStartNewGameTargetDiffersFromCurrent` FAIL

- [x] **Step 3: 修复实现**

替换 `GameEngine.startNewGame()`：

```swift
func startNewGame() {
    let bottles = (0..<bottleCount).map { index in
        Bottle(id: index, colorIndex: index, shapeIndex: index)
    }
    var target = bottles.shuffled()
    // 保证目标排列 != 自然序，否则开局即通关
    if target.map(\.id) == bottles.map(\.id) {
        target.swapAt(0, 1)
    }
    targetArray = BottleArray(bottles: target)
    currentArray = BottleArray(bottles: bottles)
    scoreCalculator.reset()
    state = .playing
}
```

- [x] **Step 4: 运行测试验证通过**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/GameEngineTests`
预期：Test Suite 'GameEngineTests' passed（全部测试，包括旧的 testSubmitCorrectAnswer——它直接赋值 `currentArray = targetArray`，不受影响）

- [x] **Step 5: 提交**

```bash
git add SortingGame/GameCore/Engine/GameEngine.swift SortingGameTests/GameCore/GameEngineTests.swift
git commit -m "fix: 玩家从自然序开始推理，目标排列保证与自然序不同"
```

---

## Task 2: GameEngine 增加选中状态与标记系统

选中状态从 GameScene 收进 GameCore（架构规则：状态只在 GameCore），键盘/鼠标/触摸共用 `tap(position:)` 一个入口。标记为位置集合，仅视觉辅助不影响判定。

**Files:**
- Modify: `SortingGame/GameCore/Engine/GameEngine.swift`
- Test: `SortingGameTests/GameCore/GameEngineTests.swift`

- [x] **Step 1: 写失败测试**

在 `GameEngineTests.swift` 追加：

```swift
func testTapSelectsThenSwaps() {
    var engine = GameEngine(bottleCount: 4)
    engine.startNewGame()

    engine.tap(position: 0)
    XCTAssertEqual(engine.selectedPosition, 0)

    engine.tap(position: 2)
    XCTAssertNil(engine.selectedPosition)
    XCTAssertEqual(engine.currentArray?.bottles[0].id, 2)
    XCTAssertEqual(engine.currentArray?.bottles[2].id, 0)
}

func testTapSamePositionDeselects() {
    var engine = GameEngine(bottleCount: 4)
    engine.startNewGame()

    engine.tap(position: 1)
    engine.tap(position: 1)
    XCTAssertNil(engine.selectedPosition)
    XCTAssertEqual(engine.currentArray?.bottles.map(\.id), [0, 1, 2, 3])
}

func testTapOutOfRangeIgnored() {
    var engine = GameEngine(bottleCount: 4)
    engine.startNewGame()

    engine.tap(position: -1)
    engine.tap(position: 4)
    XCTAssertNil(engine.selectedPosition)
}

func testTapIgnoredWhenNotPlaying() {
    var engine = GameEngine(bottleCount: 4)
    engine.tap(position: 0)
    XCTAssertNil(engine.selectedPosition)
}

func testMarkModeTogglesMarks() {
    var engine = GameEngine(bottleCount: 4)
    engine.startNewGame()
    engine.isMarkMode = true

    engine.tap(position: 1)
    XCTAssertEqual(engine.markedPositions, [1])
    XCTAssertNil(engine.selectedPosition)

    engine.tap(position: 1)
    XCTAssertEqual(engine.markedPositions, [])
}

func testStartNewGameResetsSelectionAndMarks() {
    var engine = GameEngine(bottleCount: 4)
    engine.startNewGame()
    engine.isMarkMode = true
    engine.tap(position: 0)
    engine.isMarkMode = false
    engine.tap(position: 1)

    engine.startNewGame()
    XCTAssertNil(engine.selectedPosition)
    XCTAssertEqual(engine.markedPositions, [])
    XCTAssertFalse(engine.isMarkMode)
}

func testGiveUpClearsSelection() {
    var engine = GameEngine(bottleCount: 4)
    engine.startNewGame()
    engine.tap(position: 0)
    engine.giveUp()
    XCTAssertNil(engine.selectedPosition)
}
```

- [x] **Step 2: 运行测试验证失败**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/GameEngineTests`
预期：编译错误（`tap`、`selectedPosition`、`isMarkMode`、`markedPositions` 不存在）

- [x] **Step 3: 实现**

在 `GameEngine` 中新增属性（放在 `scoreCalculator` 之后）：

```swift
private(set) var selectedPosition: Int?
private(set) var markedPositions: Set<Int> = []
/// 标记模式：开启后 tap 切换 ✓ 标记而非选择交换
var isMarkMode = false
```

新增方法：

```swift
/// 统一语义入口：鼠标点击/触摸/键盘数字键都翻译为此调用
func tap(position: Int) {
    guard state == .playing,
          position >= 0, position < bottleCount else { return }

    if isMarkMode {
        if markedPositions.contains(position) {
            markedPositions.remove(position)
        } else {
            markedPositions.insert(position)
        }
        return
    }

    if let selected = selectedPosition {
        if selected == position {
            selectedPosition = nil
        } else {
            currentArray?.swap(from: selected, to: position)
            selectedPosition = nil
        }
    } else {
        selectedPosition = position
    }
}

func clearSelection() {
    selectedPosition = nil
}
```

修改现有方法：

```swift
func startNewGame() {
    // ...原有生成逻辑（Task 1 修复后的版本）...
    selectedPosition = nil
    markedPositions = []
    isMarkMode = false
    state = .playing
}

func submitGuess() {
    guard state == .playing,
          let target = targetArray,
          let current = currentArray else { return }

    scoreCalculator.incrementAttempt()

    if current.isFullyCorrect(compareTo: target) {
        selectedPosition = nil
        state = .won(attempts: scoreCalculator.attemptCount)
    }
}

func giveUp() {
    guard state == .playing, let target = targetArray else { return }
    selectedPosition = nil
    state = .gaveUp(answer: target)
}
```

- [x] **Step 4: 运行测试验证通过**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/GameEngineTests`
预期：Test Suite 'GameEngineTests' passed

- [x] **Step 5: 提交**

```bash
git add SortingGame/GameCore/Engine/GameEngine.swift SortingGameTests/GameCore/GameEngineTests.swift
git commit -m "feat: GameEngine 增加选中状态与标记系统，统一 tap 语义入口"
```

---

## Task 3: GameScene 改为引擎状态驱动渲染（含坐标 bug 修复）

GameScene 不再持有选中状态，改为每帧 diff 引擎状态同步视觉（位置动画、选中高亮、✓ 标记）。瓶子挂到 boxNode 下修复坐标偏移。点击只翻译成 `engine.tap(position:)`。

**Files:**
- Modify: `SortingGame/Scenes/GameScene.swift`（整体重写）
- Modify: `SortingGame/Scenes/Nodes/BottleNode.swift`（`toggleMark` → `setMarked`）

- [x] **Step 1: 修改 BottleNode**

把 `toggleMark()` 替换为幂等的 `setMarked(_:)`（场景每帧同步需要幂等设置而非 toggle）：

```swift
func setMarked(_ marked: Bool) {
    isMarked = marked
    markLabel?.alpha = marked ? 1 : 0
}
```

- [x] **Step 2: 重写 GameScene**

```swift
import SpriteKit
#if canImport(UIKit)
import UIKit
#endif

/// 游戏主场景：纯渲染 GameCore 状态，输入翻译为语义事件回传引擎
final class GameScene: SKScene {
    private var engine: GameEngine!
    private var boxNode: BoxNode!
    private var bottleNodes: [BottleNode] = []
    /// 上一帧已渲染的瓶子顺序（bottleId 列表），用于 diff 触发移动动画
    private var renderedOrder: [Int] = []

    func configure(engine: GameEngine) {
        self.engine = engine
    }

    override func didMove(to view: SKView) {
        backgroundColor = .black
        setupGame()
    }

    private func setupGame() {
        removeAllChildren()
        bottleNodes = []
        renderedOrder = []
        guard let engine, let currentArray = engine.currentArray else { return }

        let slotSize = CGSize(width: 80, height: 120)
        boxNode = BoxNode(slotCount: currentArray.count, slotSize: slotSize)
        boxNode.position = CGPoint(x: frame.midX, y: frame.midY)
        addChild(boxNode)

        for (index, bottle) in currentArray.bottles.enumerated() {
            let node = BottleNode(bottle: bottle, size: slotSize)
            node.position = boxNode.getSlotPosition(at: index)
            boxNode.addChild(node)
            bottleNodes.append(node)
        }
        renderedOrder = currentArray.bottles.map(\.id)
    }

    override func update(_ currentTime: TimeInterval) {
        syncWithEngine()
    }

    /// 每帧 diff 引擎状态：位置变化播放移动动画，选中/标记直接同步
    private func syncWithEngine() {
        guard let engine, let current = engine.currentArray else { return }

        let order = current.bottles.map(\.id)
        if order != renderedOrder {
            for (index, bottle) in current.bottles.enumerated() {
                if let node = bottleNodes.first(where: { $0.bottleId == bottle.id }) {
                    let move = SKAction.move(to: boxNode.getSlotPosition(at: index), duration: 0.2)
                    move.timingMode = .easeInEaseOut
                    node.run(move, withKey: "moveToSlot")
                }
            }
            renderedOrder = order
        }

        for node in bottleNodes {
            let position = current.bottles.firstIndex(where: { $0.id == node.bottleId }) ?? -1
            node.setSelected(engine.selectedPosition == position)
            node.setMarked(engine.markedPositions.contains(position))
        }
    }

    #if os(macOS)
    override func mouseDown(with event: NSEvent) {
        handleTap(at: event.location(in: self))
    }
    #else
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        guard let touch = touches.first else { return }
        handleTap(at: touch.location(in: self))
    }
    #endif

    private func handleTap(at location: CGPoint) {
        guard let engine, engine.state == .playing else { return }
        let tappedNode = atPoint(location)

        for node in bottleNodes {
            // 命中瓶体/高光/选中环/标记等任意子节点都算点中该瓶子
            if tappedNode == node || tappedNode.parent == node || tappedNode.parent?.parent == node {
                if let position = engine.currentArray?.bottles.firstIndex(where: { $0.id == node.bottleId }) {
                    engine.tap(position: position)
                }
                return
            }
        }
    }

    /// 胜利庆祝动画（Task 12 使用）
    func runCelebration() {
        for node in bottleNodes {
            let pulse = SKAction.sequence([
                .scale(to: 1.2, duration: 0.15),
                .scale(to: 1.0, duration: 0.15)
            ])
            node.run(.repeat(pulse, count: 3))
        }
    }
}
```

- [x] **Step 3: 验证编译**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

- [x] **Step 4: 提交**

```bash
git add SortingGame/Scenes/GameScene.swift SortingGame/Scenes/Nodes/BottleNode.swift
git commit -m "refactor: GameScene 改为引擎状态驱动渲染，修复瓶子坐标偏移"
```

---

## Task 4: GameView 场景固化 + 标记模式按钮

**Files:**
- Modify: `SortingGame/UI/ViewModels/GameViewModel.swift`（新增 `toggleMarkMode()`：toggle `engine.isMarkMode` 并 `engine.clearSelection()`——View 不直接改引擎状态）
- Modify: `SortingGame/UI/Views/GameView.swift`（整体重写，键盘与结算在 Task 5/7 补充）

- [x] **Step 1: 重写 GameView**

```swift
import SwiftUI
import SpriteKit

struct GameView: View {
    @State private var viewModel: GameViewModel
    @State private var scene: GameScene
    private let onExitToMenu: () -> Void

    init(bottleCount: Int, onExitToMenu: @escaping () -> Void = {}) {
        let vm = GameViewModel(bottleCount: bottleCount)
        _viewModel = State(initialValue: vm)

        let scene = GameScene(size: CGSize(width: 800, height: 600))
        scene.scaleMode = .aspectFit
        scene.configure(engine: vm.engine)
        _scene = State(initialValue: scene)

        self.onExitToMenu = onExitToMenu
    }

    var body: some View {
        VStack(spacing: 20) {
            SpriteView(scene: scene)
                .frame(maxWidth: .infinity, maxHeight: .infinity)

            Text(viewModel.feedbackText)
                .font(.title2)
                .fontWeight(.semibold)

            buttonBar
        }
        .padding()
    }

    private var buttonBar: some View {
        HStack(spacing: 16) {
            Button("提交") {
                viewModel.submitGuess()
            }
            .buttonStyle(.borderedProminent)
            .disabled(viewModel.engine.state != .playing)

            Button(viewModel.engine.isMarkMode ? "退出标记" : "标记模式") {
                viewModel.toggleMarkMode()
            }
            .buttonStyle(.bordered)
            .tint(viewModel.engine.isMarkMode ? .green : .accentColor)

            Button("公布答案") {
                viewModel.giveUp()
            }
            .buttonStyle(.bordered)
            .tint(.red)
            .disabled(viewModel.engine.state != .playing)

            Button("返回") {
                onExitToMenu()
            }
            .buttonStyle(.bordered)
        }
    }
}
```

- [x] **Step 2: 更新 DifficultySelectView 传入 onExitToMenu**

修改 `SortingGame/UI/Views/DifficultySelectView.swift` 中的 sheet：

```swift
.sheet(isPresented: $showingGame) {
    GameView(bottleCount: selectedBottleCount, onExitToMenu: {
        showingGame = false
        dismiss()
    })
}
```

- [x] **Step 3: 验证编译**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

- [x] **Step 4: 提交**

```bash
git add SortingGame/UI/Views/GameView.swift SortingGame/UI/Views/DifficultySelectView.swift
git commit -m "feat: GameView 场景固化到 State，新增标记模式切换按钮"
```

---

## Task 5: 键盘快捷键（macOS）

数字键 1-7：选中/交换/放下；Enter：提交。通过 `GameViewModel.handleKey` 翻译为引擎语义事件。

**Files:**
- Modify: `SortingGame/UI/ViewModels/GameViewModel.swift`
- Modify: `SortingGame/UI/Views/GameView.swift`
- Test: `SortingGameTests/GameViewModelTests.swift`（新建）

- [x] **Step 1: 写失败测试**

创建 `SortingGameTests/GameViewModelTests.swift`：

```swift
import XCTest
import SwiftUI
@testable import SortingGame

@MainActor
final class GameViewModelTests: XCTestCase {
    func testDigitKeySelectsThenSwaps() {
        let vm = GameViewModel(bottleCount: 4)

        XCTAssertTrue(vm.handleKey(KeyEquivalent("1")))
        XCTAssertEqual(vm.engine.selectedPosition, 0)

        XCTAssertTrue(vm.handleKey(KeyEquivalent("3")))
        XCTAssertNil(vm.engine.selectedPosition)
        XCTAssertEqual(vm.engine.currentArray?.bottles[0].id, 2)
        XCTAssertEqual(vm.engine.currentArray?.bottles[2].id, 0)
    }

    func testSameDigitDeselects() {
        let vm = GameViewModel(bottleCount: 4)
        vm.handleKey(KeyEquivalent("2"))
        vm.handleKey(KeyEquivalent("2"))
        XCTAssertNil(vm.engine.selectedPosition)
    }

    func testEnterSubmits() {
        let vm = GameViewModel(bottleCount: 4)
        XCTAssertTrue(vm.handleKey(.return))
        XCTAssertEqual(vm.engine.scoreCalculator.attemptCount, 1)
        XCTAssertEqual(vm.feedbackText, "对了 0 个")
    }

    func testDigitBeyondBottleCountIgnored() {
        let vm = GameViewModel(bottleCount: 4)
        XCTAssertFalse(vm.handleKey(KeyEquivalent("5")))
        XCTAssertNil(vm.engine.selectedPosition)
    }

    func testNonDigitKeyIgnored() {
        let vm = GameViewModel(bottleCount: 4)
        XCTAssertFalse(vm.handleKey(KeyEquivalent("a")))
    }
}
```

注意：`testEnterSubmits` 依赖 Task 1 的保证（自然序 ≠ 目标），"对了 0 个" 不一定成立——自然序可能有部分位置碰巧正确。将断言改为只验证提交行为：

```swift
func testEnterSubmits() {
    let vm = GameViewModel(bottleCount: 4)
    XCTAssertTrue(vm.handleKey(.return))
    XCTAssertEqual(vm.engine.scoreCalculator.attemptCount, 1)
    XCTAssertFalse(vm.feedbackText.isEmpty)
}
```

（以修改后的版本为准创建测试文件。）

- [x] **Step 2: 运行测试验证失败**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/GameViewModelTests`
预期：编译错误（`handleKey` 不存在）

- [x] **Step 3: 实现 GameViewModel.handleKey**

在 `GameViewModel` 中追加：

```swift
#if os(macOS)
/// macOS 键盘输入：数字键 1-7 选择/交换/放下，Enter 提交。返回是否消费了该按键。
func handleKey(_ key: KeyEquivalent) -> Bool {
    guard engine.state == .playing else { return false }

    if key == .return {
        submitGuess()
        return true
    }
    guard let digit = key.character.wholeNumberValue,
          digit >= 1, digit <= engine.bottleCount else { return false }

    engine.tap(position: digit - 1)
    return true
}
#endif
```

- [x] **Step 4: GameView 接入按键监听（macOS）**

在 `GameView.body` 的 `.padding()` 之后追加：

```swift
#if os(macOS)
.focusable()
.focusEffectDisabled()
.onKeyPress(phases: .down) { press in
    viewModel.handleKey(press.key) ? .handled : .ignored
}
#endif
```

- [x] **Step 5: 运行测试验证通过 + 编译**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/GameViewModelTests`
预期：Test Suite 'GameViewModelTests' passed

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

- [x] **Step 6: 提交**

```bash
git add SortingGame/UI/ViewModels/GameViewModel.swift SortingGame/UI/Views/GameView.swift SortingGameTests/GameViewModelTests.swift
git commit -m "feat: macOS 键盘快捷键（数字键选择交换、Enter 提交）"
```

---

## Task 6: SettingsService + SettingsView + 瓶子形状符号（色盲友好）

**Files:**
- Create: `SortingGame/Services/SettingsService.swift`
- Create: `SortingGame/UI/Views/SettingsView.swift`
- Modify: `SortingGame/UI/Views/MainMenuView.swift`（接入设置 + 关于）
- Modify: `SortingGame/Scenes/Nodes/BottleNode.swift`（形状符号）
- Modify: `SortingGame/Scenes/GameScene.swift`（读取色盲模式）
- Test: `SortingGameTests/Services/SettingsServiceTests.swift`（新建）

- [x] **Step 1: 写失败测试**

创建 `SortingGameTests/Services/SettingsServiceTests.swift`：

```swift
import XCTest
@testable import SortingGame

@MainActor
final class SettingsServiceTests: XCTestCase {
    private func makeDefaults() -> UserDefaults {
        UserDefaults(suiteName: "SettingsServiceTests.\(UUID().uuidString)")!
    }

    func testDefaultValues() {
        let service = SettingsService(defaults: makeDefaults())
        XCTAssertTrue(service.soundEnabled)
        XCTAssertTrue(service.musicEnabled)
        XCTAssertTrue(service.colorblindMode)
        XCTAssertEqual(service.playerName, "玩家")
    }

    func testValuesPersistToDefaults() {
        let defaults = makeDefaults()
        let service = SettingsService(defaults: defaults)
        service.soundEnabled = false
        service.playerName = "小明"

        let reloaded = SettingsService(defaults: defaults)
        XCTAssertFalse(reloaded.soundEnabled)
        XCTAssertEqual(reloaded.playerName, "小明")
    }
}
```

- [x] **Step 2: 运行测试验证失败**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/SettingsServiceTests`
预期：编译错误（`SettingsService` 不存在）

- [x] **Step 3: 实现 SettingsService**

创建 `SortingGame/Services/SettingsService.swift`：

```swift
import Foundation
import Observation

/// 用户设置持久化（UserDefaults）
@MainActor
@Observable
final class SettingsService {
    static let shared = SettingsService()

    var soundEnabled: Bool {
        didSet { defaults.set(soundEnabled, forKey: Keys.sound) }
    }
    var musicEnabled: Bool {
        didSet { defaults.set(musicEnabled, forKey: Keys.music) }
    }
    /// 色盲模式：强化瓶子形状差异（默认开启）
    var colorblindMode: Bool {
        didSet { defaults.set(colorblindMode, forKey: Keys.colorblind) }
    }
    /// 排行榜用昵称
    var playerName: String {
        didSet { defaults.set(playerName, forKey: Keys.name) }
    }

    private let defaults: UserDefaults

    private enum Keys {
        static let sound = "settings.soundEnabled"
        static let music = "settings.musicEnabled"
        static let colorblind = "settings.colorblindMode"
        static let name = "settings.playerName"
    }

    init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
        self.soundEnabled = defaults.object(forKey: Keys.sound) as? Bool ?? true
        self.musicEnabled = defaults.object(forKey: Keys.music) as? Bool ?? true
        self.colorblindMode = defaults.object(forKey: Keys.colorblind) as? Bool ?? true
        self.playerName = defaults.string(forKey: Keys.name) ?? "玩家"
    }
}
```

- [x] **Step 4: 运行测试验证通过**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/SettingsServiceTests`
预期：Test Suite 'SettingsServiceTests' passed

- [x] **Step 5: 提交**

```bash
git add SortingGame/Services/SettingsService.swift SortingGameTests/Services/SettingsServiceTests.swift
git commit -m "feat: 添加 SettingsService 设置持久化"
```

- [x] **Step 6: 实现 SettingsView**

创建 `SortingGame/UI/Views/SettingsView.swift`：

```swift
import SwiftUI

struct SettingsView: View {
    @Environment(\.dismiss) private var dismiss
    @State private var settings = SettingsService.shared

    var body: some View {
        @Bindable var settings = settings

        VStack(alignment: .leading, spacing: 16) {
            Text("设置")
                .font(.title)
                .bold()

            Toggle("音效", isOn: $settings.soundEnabled)
            Toggle("音乐", isOn: $settings.musicEnabled)
            Toggle("色盲模式（强化形状差异）", isOn: $settings.colorblindMode)
            TextField("昵称", text: $settings.playerName)
                .textFieldStyle(.roundedBorder)

            #if os(macOS)
            VStack(alignment: .leading, spacing: 4) {
                Text("键盘快捷键")
                    .font(.headline)
                Text("数字键 1-7：选中瓶子 / 交换 / 放下")
                Text("Enter：提交当前排列")
            }
            .font(.callout)
            .foregroundStyle(.secondary)
            #endif

            Button("完成") { dismiss() }
                .buttonStyle(.borderedProminent)
                .frame(maxWidth: .infinity, alignment: .center)
        }
        .padding(24)
        .frame(minWidth: 340)
    }
}

#Preview {
    SettingsView()
}
```

- [x] **Step 7: MainMenuView 接入设置与关于**

重写 `MainMenuView` 的两个占位 sheet 并新增"关于"：

```swift
import SwiftUI

struct MainMenuView: View {
    @State private var showingDifficultySelect = false
    @State private var showingLeaderboard = false
    @State private var showingSettings = false
    @State private var showingAbout = false

    var body: some View {
        VStack(spacing: 30) {
            Text("SortingGame")
                .font(.largeTitle)
                .fontWeight(.bold)

            VStack(spacing: 15) {
                Button("开始游戏") {
                    showingDifficultySelect = true
                }
                .buttonStyle(.borderedProminent)

                Button("排行榜") {
                    showingLeaderboard = true
                }
                .buttonStyle(.bordered)

                Button("设置") {
                    showingSettings = true
                }
                .buttonStyle(.bordered)

                Button("关于") {
                    showingAbout = true
                }
                .buttonStyle(.bordered)
            }
        }
        .padding()
        .sheet(isPresented: $showingDifficultySelect) {
            DifficultySelectView()
        }
        .sheet(isPresented: $showingLeaderboard) {
            Text("排行榜（待实现）")
                .padding()
        }
        .sheet(isPresented: $showingSettings) {
            SettingsView()
        }
        .sheet(isPresented: $showingAbout) {
            AboutView()
        }
    }
}

struct AboutView: View {
    @Environment(\.dismiss) private var dismiss

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("关于 SortingGame")
                .font(.title)
                .bold()
            Text("系统随机生成一组瓶子的隐藏排列，你通过交换瓶子来推理还原正确顺序。")
            Text("每次提交后系统只告诉你「对了 x 个」——位置正确的瓶子数量，不告诉你具体哪些位置正确。")
            Text("尝试次数越少越厉害，通关成绩会记入本地排行榜（公布答案的成绩不计入）。")
            Text("标记模式（✓）可以帮你记住已推理确定的位置，仅作视觉辅助。")
            Button("关闭") { dismiss() }
                .buttonStyle(.borderedProminent)
        }
        .padding(24)
        .frame(maxWidth: 420)
    }
}

#Preview {
    MainMenuView()
}
```

（排行榜 sheet 在 Task 9 替换为 LeaderboardView。）

- [x] **Step 8: BottleNode 增加形状符号**

修改 `BottleNode`：init 签名增加 `colorblindMode` 参数，瓶身中央叠加形状符号：

```swift
private static let shapeSymbols = ["●", "■", "▲", "◆", "★", "⬟", "✚"]

init(bottle: Bottle, size: CGSize, colorblindMode: Bool) {
    // ...原有属性赋值与瓶身/高光/选中环/标记创建保持不变...

    // 形状符号（色盲友好：颜色+形状双重区分）
    let symbol = SKLabelNode(text: Self.shapeSymbols[bottle.shapeIndex % Self.shapeSymbols.count])
    symbol.fontSize = size.width * (colorblindMode ? 0.55 : 0.4)
    symbol.fontColor = Self.symbolColor(forBodyColor: Self.colorForIndex(bottle.colorIndex))
    symbol.horizontalAlignmentMode = .center
    symbol.verticalAlignmentMode = .center
    symbol.position = .zero
    bottleBody.addChild(symbol)

    super.init()
    // ...原有 addChild 保持不变...
}

/// 深色瓶体用白符号，浅色（黄/青）用黑符号，保证对比度
private static func symbolColor(forBodyColor color: SKColor) -> SKColor {
    var red: CGFloat = 0, green: CGFloat = 0, blue: CGFloat = 0, alpha: CGFloat = 0
    color.getRed(&red, green: &green, blue: &blue, alpha: &alpha)
    let luminance = 0.299 * red + 0.587 * green + 0.114 * blue
    return luminance > 0.6 ? .black : .white
}
```

- [x] **Step 9: GameScene 传入色盲模式**

修改 `GameScene.setupGame()` 中创建节点处：

```swift
let colorblindMode = SettingsService.shared.colorblindMode
for (index, bottle) in currentArray.bottles.enumerated() {
    let node = BottleNode(bottle: bottle, size: slotSize, colorblindMode: colorblindMode)
    node.position = boxNode.getSlotPosition(at: index)
    boxNode.addChild(node)
    bottleNodes.append(node)
}
```

（Scene 在主线程运行，直接读 `@MainActor` 的 SettingsService.shared。若严格并发报错，用 `MainActor.assumeIsolated { SettingsService.shared.colorblindMode }` 包裹。）

- [x] **Step 10: 验证编译 + 全部测试**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test`
预期：所有测试通过

- [x] **Step 11: 提交**

```bash
git add SortingGame/UI/Views/SettingsView.swift SortingGame/UI/Views/MainMenuView.swift SortingGame/Scenes/Nodes/BottleNode.swift SortingGame/Scenes/GameScene.swift
git commit -m "feat: 设置界面（音效/音乐/色盲模式/昵称）与瓶子形状符号"
```

---

## Task 7: SettlementView 结算界面（含公布答案对比）+ GameView 集成

胜利显示尝试次数；放弃显示正确答案与玩家排列对比（逐位标绿/红）。

**Files:**
- Create: `SortingGame/UI/Views/SettlementView.swift`
- Create: `SortingGame/UI/BottleStyle.swift`
- Modify: `SortingGame/UI/Views/GameView.swift`

- [x] **Step 1: 创建共享的瓶子视觉样式**

创建 `SortingGame/UI/BottleStyle.swift`（SwiftUI 侧与 BottleNode 的颜色/符号保持一致）：

```swift
import SwiftUI

/// 瓶子的 SwiftUI 视觉样式（与 SpriteKit 的 BottleNode 保持一致）
enum BottleStyle {
    static let shapeSymbols = ["●", "■", "▲", "◆", "★", "⬟", "✚"]

    static func shapeSymbol(forIndex index: Int) -> String {
        shapeSymbols[index % shapeSymbols.count]
    }

    static func color(forIndex index: Int) -> Color {
        let colors: [Color] = [.red, .blue, .green, .orange, .purple, .cyan, .yellow]
        return colors[index % colors.count]
    }
}

/// 结算界面用的瓶子圆形徽章
struct BottleChip: View {
    let bottle: Bottle
    /// nil = 不显示对错圈；true = 绿圈；false = 红圈
    var isCorrect: Bool? = nil

    var body: some View {
        ZStack {
            Circle()
                .fill(BottleStyle.color(forIndex: bottle.colorIndex))
                .frame(width: 36, height: 36)
            Text(BottleStyle.shapeSymbol(forIndex: bottle.shapeIndex))
                .font(.system(size: 16))
                .foregroundStyle(.white)
        }
        .overlay {
            if let isCorrect {
                Circle()
                    .stroke(isCorrect ? Color.green : Color.red, lineWidth: 3)
            }
        }
    }
}
```

- [x] **Step 2: 创建 SettlementView**

创建 `SortingGame/UI/Views/SettlementView.swift`（排行榜保存在 Task 10 接入，此处只留扩展位）：

```swift
import SwiftUI

/// 结算界面：胜利显示尝试次数，放弃显示答案对比
struct SettlementView: View {
    let state: GameState
    let playerArray: BottleArray
    let onPlayAgain: () -> Void
    let onChangeDifficulty: () -> Void
    let onBackToMenu: () -> Void

    var body: some View {
        VStack(spacing: 20) {
            switch state {
            case .won(let attempts):
                wonContent(attempts: attempts)
            case .gaveUp(let answer):
                gaveUpContent(answer: answer)
            case .preparing, .playing:
                EmptyView()
            }

            HStack(spacing: 16) {
                Button("再来一局", action: onPlayAgain)
                    .buttonStyle(.borderedProminent)
                Button("更换难度", action: onChangeDifficulty)
                    .buttonStyle(.bordered)
                Button("返回主菜单", action: onBackToMenu)
                    .buttonStyle(.bordered)
            }
        }
        .padding(24)
        .frame(minWidth: 400)
    }

    private func wonContent(attempts: Int) -> some View {
        VStack(spacing: 8) {
            Text("恭喜通关！")
                .font(.largeTitle)
                .bold()
            Text("尝试次数：\(attempts)")
                .font(.title2)
        }
    }

    private func gaveUpContent(answer: BottleArray) -> some View {
        VStack(spacing: 12) {
            Text("正确答案")
                .font(.largeTitle)
                .bold()

            VStack(spacing: 8) {
                Text("正确排列")
                    .font(.headline)
                HStack(spacing: 8) {
                    ForEach(answer.bottles) { bottle in
                        BottleChip(bottle: bottle)
                    }
                }
            }

            VStack(spacing: 8) {
                Text("你的排列（绿=位置正确，红=位置错误）")
                    .font(.headline)
                HStack(spacing: 8) {
                    ForEach(Array(playerArray.bottles.enumerated()), id: \.element.id) { index, bottle in
                        BottleChip(
                            bottle: bottle,
                            isCorrect: index < answer.bottles.count
                                && answer.bottles[index].id == bottle.id
                        )
                    }
                }
            }
        }
    }
}
```

- [x] **Step 3: GameView 集成结算 sheet**

在 `GameView` 中新增状态与 onChange（放在 `.padding()` 之后、`#if os(macOS)` 之前）。注意：Task 4 清理时删除了 `@Environment(\.dismiss)`，此处的"更换难度"需要它关闭 GameView 回到难度选择 sheet，需重新加回 `@Environment(\.dismiss) private var dismiss`：

```swift
@State private var showingSettlement = false
```

body 修饰符追加：

```swift
.onChange(of: viewModel.engine.state) { _, newState in
    switch newState {
    case .won, .gaveUp:
        showingSettlement = true
    case .preparing, .playing:
        break
    }
}
.sheet(isPresented: $showingSettlement) {
    SettlementView(
        state: viewModel.engine.state,
        playerArray: viewModel.engine.currentArray ?? BottleArray(bottles: []),
        onPlayAgain: {
            showingSettlement = false
            viewModel.startNewGame()
        },
        onChangeDifficulty: {
            showingSettlement = false
            dismiss()
        },
        onBackToMenu: {
            showingSettlement = false
            onExitToMenu()
        }
    )
}
```

说明：`onChange(of:)` 在 body 中读取 `engine.state`，@Observable 会自动注册观察——无论状态由按钮（提交/公布答案）还是场景交互触发都能捕获。再来一局无需重建场景：新局的瓶子 id 集合不变，GameScene 每帧 diff 会自动把瓶子动画归位到自然序。

- [x] **Step 4: 验证编译**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

- [x] **Step 5: 手动验证（在 Xcode 运行 macOS app）**

流程：主菜单 → 简单(4瓶) → 瓶子应从自然序开始（非答案）→ 点击/键盘交换有动画 → 标记模式切换 ✓ → 提交显示"对了 x 个" → 公布答案弹结算显示对比 → 再来一局重置 → 返回主菜单关闭两层 sheet。

- [x] **Step 6: 提交**

```bash
git add SortingGame/UI/BottleStyle.swift SortingGame/UI/Views/SettlementView.swift SortingGame/UI/Views/GameView.swift
git commit -m "feat: 结算界面（胜利尝试次数、放弃答案对比）并集成到游戏流程"
```

---

# Phase 3：排行榜

## Task 8: LeaderboardService 本地排行榜存储

**Files:**
- Create: `SortingGame/Services/LeaderboardService.swift`
- Test: `SortingGameTests/Services/LeaderboardServiceTests.swift`（新建）

- [x] **Step 1: 写失败测试**

创建 `SortingGameTests/Services/LeaderboardServiceTests.swift`：

```swift
import XCTest
@testable import SortingGame

@MainActor
final class LeaderboardServiceTests: XCTestCase {
    private func makeStorageURL() -> URL {
        FileManager.default.temporaryDirectory
            .appendingPathComponent("leaderboard-\(UUID().uuidString).json")
    }

    func testEmptyEntries() {
        let service = LeaderboardService(storageURL: makeStorageURL())
        XCTAssertEqual(service.topEntries(bottleCount: 4), [])
    }

    func testAddEntryReturnsRank() {
        let service = LeaderboardService(storageURL: makeStorageURL())
        XCTAssertEqual(service.addEntry(bottleCount: 4, attempts: 5, playerName: "A"), 1)
        XCTAssertEqual(service.addEntry(bottleCount: 4, attempts: 3, playerName: "B"), 1)
        XCTAssertEqual(service.addEntry(bottleCount: 4, attempts: 4, playerName: "C"), 3)
    }

    func testTopEntriesSortedByAttempts() {
        let service = LeaderboardService(storageURL: makeStorageURL())
        service.addEntry(bottleCount: 5, attempts: 9, playerName: "A")
        service.addEntry(bottleCount: 5, attempts: 2, playerName: "B")
        service.addEntry(bottleCount: 5, attempts: 5, playerName: "C")

        let top = service.topEntries(bottleCount: 5)
        XCTAssertEqual(top.map(\.attempts), [2, 5, 9])
        XCTAssertEqual(top.map(\.playerName), ["B", "C", "A"])
    }

    func testSeparateBoardsPerBottleCount() {
        let service = LeaderboardService(storageURL: makeStorageURL())
        service.addEntry(bottleCount: 4, attempts: 1, playerName: "A")
        service.addEntry(bottleCount: 7, attempts: 2, playerName: "B")

        XCTAssertEqual(service.topEntries(bottleCount: 4).map(\.playerName), ["A"])
        XCTAssertEqual(service.topEntries(bottleCount: 7).map(\.playerName), ["B"])
    }

    func testTopEntriesLimitedToTen() {
        let service = LeaderboardService(storageURL: makeStorageURL())
        for attempts in 1...15 {
            service.addEntry(bottleCount: 4, attempts: attempts, playerName: "P\(attempts)")
        }
        XCTAssertEqual(service.topEntries(bottleCount: 4).count, 10)
        XCTAssertEqual(service.topEntries(bottleCount: 4).map(\.attempts), Array(1...10))
    }

    func testRankNilWhenOutsideTopTen() {
        let service = LeaderboardService(storageURL: makeStorageURL())
        for attempts in 1...10 {
            service.addEntry(bottleCount: 4, attempts: attempts, playerName: "P\(attempts)")
        }
        let rank = service.addEntry(bottleCount: 4, attempts: 99, playerName: "Slow")
        XCTAssertNil(rank)
    }

    func testPersistenceAcrossInstances() throws {
        let url = makeStorageURL()
        defer { try? FileManager.default.removeItem(at: url) }

        let service1 = LeaderboardService(storageURL: url)
        service1.addEntry(bottleCount: 6, attempts: 3, playerName: "A")

        let service2 = LeaderboardService(storageURL: url)
        XCTAssertEqual(service2.topEntries(bottleCount: 6).map(\.playerName), ["A"])
    }
}
```

- [x] **Step 2: 运行测试验证失败**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/LeaderboardServiceTests`
预期：编译错误（`LeaderboardService` 不存在）

- [x] **Step 3: 实现**

创建 `SortingGame/Services/LeaderboardService.swift`：

```swift
import Foundation
import Observation

/// 排行榜单条记录（仅成功通关计入）
struct LeaderboardEntry: Codable, Equatable, Identifiable, Sendable {
    let id: UUID
    let bottleCount: Int
    let attempts: Int
    let completedAt: Date
    let playerName: String
}

/// 本地排行榜：按瓶子数量分榜，尝试次数少者靠前，JSON 文件持久化
@MainActor
@Observable
final class LeaderboardService {
    static let shared = LeaderboardService()

    private(set) var entries: [LeaderboardEntry] = []
    private let storageURL: URL

    init(storageURL: URL? = nil) {
        if let storageURL {
            self.storageURL = storageURL
        } else {
            let directory = FileManager.default
                .urls(for: .applicationSupportDirectory, in: .userDomainMask)[0]
                .appendingPathComponent("SortingGame", isDirectory: true)
            self.storageURL = directory.appendingPathComponent("leaderboard.json")
        }
        load()
    }

    /// 添加成绩，返回该难度榜内排名（1 起；未进 Top 10 返回 nil）
    @discardableResult
    func addEntry(bottleCount: Int, attempts: Int, playerName: String) -> Int? {
        let entry = LeaderboardEntry(
            id: UUID(),
            bottleCount: bottleCount,
            attempts: attempts,
            completedAt: Date(),
            playerName: playerName
        )
        entries.append(entry)
        save()
        return rank(of: entry)
    }

    func rank(of entry: LeaderboardEntry) -> Int? {
        topEntries(bottleCount: entry.bottleCount)
            .firstIndex(where: { $0.id == entry.id })
            .map { $0 + 1 }
    }

    /// 指定难度的 Top N（默认 10），尝试次数少者靠前，同次数按完成时间早者靠前
    func topEntries(bottleCount: Int, limit: Int = 10) -> [LeaderboardEntry] {
        Array(
            entries
                .filter { $0.bottleCount == bottleCount }
                .sorted { lhs, rhs in
                    if lhs.attempts != rhs.attempts {
                        return lhs.attempts < rhs.attempts
                    }
                    return lhs.completedAt < rhs.completedAt
                }
                .prefix(limit)
        )
    }

    private func load() {
        guard let data = try? Data(contentsOf: storageURL),
              let decoded = try? JSONDecoder().decode([LeaderboardEntry].self, from: data) else { return }
        entries = decoded
    }

    private func save() {
        let directory = storageURL.deletingLastPathComponent()
        try? FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
        guard let data = try? JSONEncoder().encode(entries) else { return }
        try? data.write(to: storageURL, options: .atomic)
    }
}
```

- [x] **Step 4: 运行测试验证通过**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test -only-testing:SortingGameTests/LeaderboardServiceTests`
预期：Test Suite 'LeaderboardServiceTests' passed

- [x] **Step 5: 提交**

```bash
git add SortingGame/Services/LeaderboardService.swift SortingGameTests/Services/LeaderboardServiceTests.swift
git commit -m "feat: 添加 LeaderboardService 本地排行榜存储"
```

---

## Task 9: LeaderboardView + 主菜单接入

**Files:**
- Create: `SortingGame/UI/Views/LeaderboardView.swift`
- Modify: `SortingGame/UI/Views/MainMenuView.swift`

- [x] **Step 1: 实现 LeaderboardView**

创建 `SortingGame/UI/Views/LeaderboardView.swift`：

```swift
import SwiftUI

/// 排行榜：按瓶子数量分榜，显示 Top 10
struct LeaderboardView: View {
    @Environment(\.dismiss) private var dismiss
    @State private var selectedCount = 4
    private var service = LeaderboardService.shared

    var body: some View {
        VStack(spacing: 16) {
            Text("排行榜")
                .font(.title)
                .bold()

            Picker("难度", selection: $selectedCount) {
                Text("简单(4)").tag(4)
                Text("中等(5)").tag(5)
                Text("困难(6)").tag(6)
                Text("专家(7)").tag(7)
            }
            .pickerStyle(.segmented)
            .labelsHidden()

            entryList

            Button("关闭") { dismiss() }
                .buttonStyle(.bordered)
        }
        .padding(24)
        .frame(minWidth: 440, minHeight: 420)
    }

    @ViewBuilder
    private var entryList: some View {
        let entries = service.topEntries(bottleCount: selectedCount)
        if entries.isEmpty {
            ContentUnavailableView("暂无记录", systemImage: "trophy", description: Text("通关后成绩会自动记录在这里"))
        } else {
            List {
                ForEach(Array(entries.enumerated()), id: \.element.id) { index, entry in
                    HStack {
                        Text("\(index + 1)")
                            .font(.headline)
                            .frame(width: 28, alignment: .leading)
                        Text(entry.playerName)
                        Spacer()
                        Text("\(entry.attempts) 次")
                            .monospacedDigit()
                        Text(entry.completedAt, format: .dateTime.month().day())
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                }
            }
            .listStyle(.inset)
        }
    }
}

#Preview {
    LeaderboardView()
}
```

- [x] **Step 2: MainMenuView 替换排行榜占位**

把 `MainMenuView` 中：

```swift
.sheet(isPresented: $showingLeaderboard) {
    Text("排行榜（待实现）")
        .padding()
}
```

替换为：

```swift
.sheet(isPresented: $showingLeaderboard) {
    LeaderboardView()
}
```

- [x] **Step 3: 验证编译**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

- [x] **Step 4: 提交**

```bash
git add SortingGame/UI/Views/LeaderboardView.swift SortingGame/UI/Views/MainMenuView.swift
git commit -m "feat: 排行榜界面（按瓶子数量分榜 Top 10）"
```

---

## Task 10: 结算界面接入排行榜（胜利自动保存 + 显示排名）

**Files:**
- Modify: `SortingGame/UI/Views/SettlementView.swift`
- Modify: `SortingGame/UI/Views/GameView.swift`（结算 sheet 增加"查看排行榜"入口可选）

- [x] **Step 1: SettlementView 胜利时保存成绩**

修改 `SettlementView`：

新增属性：

```swift
@State private var savedRank: Int?
@State private var scoreSaved = false
```

修改 `wonContent`：

```swift
private func wonContent(attempts: Int) -> some View {
    VStack(spacing: 8) {
        Text("恭喜通关！")
            .font(.largeTitle)
            .bold()
        Text("尝试次数：\(attempts)")
            .font(.title2)
        if let savedRank {
            Text("已记入 \(bottleCount) 瓶榜：第 \(savedRank) 名")
                .foregroundStyle(.secondary)
        }
    }
    .onAppear {
        // 防止 sheet 重复触发 onAppear 造成重复写入
        guard !scoreSaved else { return }
        scoreSaved = true
        savedRank = LeaderboardService.shared.addEntry(
            bottleCount: bottleCount,
            attempts: attempts,
            playerName: SettingsService.shared.playerName
        )
    }
}
```

同时给 `SettlementView` 增加 `let bottleCount: Int` 属性（放在 `state` 之后）。

- [x] **Step 2: GameView 传入 bottleCount**

修改 `GameView` 中 SettlementView 构造，在 `state:` 之后加一行：

```swift
bottleCount: viewModel.engine.bottleCount,
```

- [x] **Step 3: 验证编译**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

- [x] **Step 4: 手动验证**

通关一局（可临时用 4 瓶 + 多次提交），确认结算显示"已记入 4 瓶榜：第 N 名"；打开主菜单排行榜能看到记录；公布答案的局不出现在排行榜。

- [x] **Step 5: 提交**

```bash
git add SortingGame/UI/Views/SettlementView.swift SortingGame/UI/Views/GameView.swift
git commit -m "feat: 通关成绩自动记入排行榜并显示排名"
```

---

# Phase 4：打磨

## Task 11: SoundService 音效

使用系统音效实现（无需音频资产），受设置音效开关控制。**BGM 需要用户提供音频文件，本计划仅实现音效，音乐开关保留在设置中、BGM 播放列为后续资产到位后的任务。**

**Files:**
- Create: `SortingGame/Services/SoundEffectPlayer.swift`
- Modify: `SortingGame/UI/ViewModels/GameViewModel.swift`（提交音效）
- Modify: `SortingGame/UI/Views/GameView.swift`（胜利音效）
- Modify: `SortingGame/Scenes/GameScene.swift`（交换音效）

- [x] **Step 1: 实现 SoundEffectPlayer**

创建 `SortingGame/Services/SoundEffectPlayer.swift`：

```swift
import Foundation
#if os(iOS)
import AudioToolbox
#else
import AppKit
#endif

enum SoundEffect: String {
    case swap
    case submit
    case win
}

/// 音效播放（系统音效，受设置的音效开关控制）
@MainActor
enum SoundEffectPlayer {
    static func play(_ effect: SoundEffect) {
        guard SettingsService.shared.soundEnabled else { return }

        #if os(iOS)
        let soundIDs: [SoundEffect: SystemSoundID] = [
            .swap: 1104,     // Tock
            .submit: 1105,   // Tink
            .win: 1025       // fanfare-ish
        ]
        AudioServicesPlaySystemSound(soundIDs[effect] ?? 1104)
        #else
        switch effect {
        case .swap:
            NSSound.beep()
        case .submit:
            NSSound(named: .funk)?.play()
        case .win:
            NSSound(named: .glass)?.play()
        }
        #endif
    }
}
```

- [x] **Step 2: 接入调用点**

`GameViewModel.submitGuess()`：

```swift
func submitGuess() {
    engine.submitGuess()
    updateFeedback()
    if case .won = engine.state {
        SoundEffectPlayer.play(.win)
    } else {
        SoundEffectPlayer.play(.submit)
    }
}
```

`GameScene.syncWithEngine()` 中检测到顺序变化时（`if order != renderedOrder` 分支内、更新 `renderedOrder` 之前）追加：

```swift
if !renderedOrder.isEmpty {
    SoundEffectPlayer.play(.swap)
}
```

（`renderedOrder.isEmpty` 时是首次布局，不播交换音。若 SKScene 非 MainActor 隔离导致编译错误，用 `MainActor.assumeIsolated { SoundEffectPlayer.play(.swap) }` 包裹。）

- [x] **Step 3: 验证编译 + 测试**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test`
预期：所有测试通过

- [x] **Step 4: 提交**

```bash
git add SortingGame/Services/SoundEffectPlayer.swift SortingGame/UI/ViewModels/GameViewModel.swift SortingGame/Scenes/GameScene.swift
git commit -m "feat: 交换/提交/胜利音效（系统音效，受设置开关控制）"
```

---

## Task 12: 动画优化

**Files:**
- Modify: `SortingGame/UI/Views/GameView.swift`（反馈文字动画、胜利触发庆祝）
- Modify: `SortingGame/Scenes/GameScene.swift`（runCelebration 已在 Task 3 就位）

- [x] **Step 1: 反馈文字弹性动画**

修改 `GameView` 中反馈 Text：

```swift
Text(viewModel.feedbackText)
    .font(.title2)
    .fontWeight(.semibold)
    .id(viewModel.feedbackText)
    .transition(.scale.combined(with: .opacity))
    .animation(.spring(duration: 0.3), value: viewModel.feedbackText)
```

- [x] **Step 2: 胜利触发场景庆祝动画**

修改 `GameView` 的 `onChange(of: viewModel.engine.state)`：

```swift
.onChange(of: viewModel.engine.state) { _, newState in
    switch newState {
    case .won:
        scene.runCelebration()
        showingSettlement = true
    case .gaveUp:
        showingSettlement = true
    case .preparing, .playing:
        break
    }
}
```

为避免庆祝动画被结算 sheet 立刻遮挡，延迟弹结算：

```swift
case .won:
    scene.runCelebration()
    Task {
        try? await Task.sleep(for: .seconds(1.2))
        showingSettlement = true
    }
```

（`.gaveUp` 分支保持立即弹出。）

- [x] **Step 3: 验证编译**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

- [x] **Step 4: 提交**

```bash
git add SortingGame/UI/Views/GameView.swift
git commit -m "feat: 反馈文字弹性动画与胜利庆祝动画"
```

---

## Task 13: iOS target 接入共享代码

当前 `SortingGame-iOS` target 只同步 `SortingGame-iOS/` 文件夹（仍是模板 Hello World）。通过 pbxproj 把 `SortingGame/` 共享文件夹加入 iOS target，并用 membershipExceptions 排除 macOS 的 App 入口。同时删除 macOS target 里未使用的模板 `ContentView.swift`。

**Files:**
- Modify: `SortingGame.xcodeproj/project.pbxproj`
- Delete: `SortingGame/ContentView.swift`（未使用的模板文件）
- Modify: `SortingGame-iOS/SortingGame_iOSApp.swift`
- Delete: `SortingGame-iOS/ContentView.swift`

- [x] **Step 1: 删除未使用的模板文件**

```bash
rm SortingGame/ContentView.swift SortingGame-iOS/ContentView.swift
```

- [x] **Step 2: 修改 pbxproj —— iOS target 增加共享 group**

在 `project.pbxproj` 中找到 `SortingGame-iOS` target（ID `77672790305FCB2D00445BE3`）的 `fileSystemSynchronizedGroups`：

```
			fileSystemSynchronizedGroups = (
				77672792305FCB2D00445BE3 /* SortingGame-iOS */,
			);
```

改为（追加共享文件夹 group，新异常集 ID 用 `77A10001305FC75200445B01`）：

```
			fileSystemSynchronizedGroups = (
				77672792305FCB2D00445BE3 /* SortingGame-iOS */,
				7767272F305FC75200445BE3 /* SortingGame */,
			);
```

- [x] **Step 3: 修改 pbxproj —— 共享 group 挂异常集，排除 macOS 入口**

找到 `PBXFileSystemSynchronizedRootGroup` 段中：

```
		7767272F305FC75200445BE3 /* SortingGame */ = {
			isa = PBXFileSystemSynchronizedRootGroup;
			path = SortingGame;
			sourceTree = "<group>";
		};
```

改为：

```
		7767272F305FC75200445BE3 /* SortingGame */ = {
			isa = PBXFileSystemSynchronizedRootGroup;
			exceptions = (
				77A10001305FC75200445B01 /* PBXFileSystemSynchronizedBuildFileExceptionSet */,
			);
			path = SortingGame;
			sourceTree = "<group>";
		};
```

在文件中（`PBXFileSystemSynchronizedRootGroup` 段之前）新增一段：

```
/* Begin PBXFileSystemSynchronizedBuildFileExceptionSet section */
		77A10001305FC75200445B01 /* Exceptions for "SortingGame" folder in "SortingGame-iOS" target */ = {
			isa = PBXFileSystemSynchronizedBuildFileExceptionSet;
			membershipExceptions = (
				SortingGameApp.swift,
			);
			target = 77672790305FCB2D00445BE3 /* SortingGame-iOS */;
		};
/* End PBXFileSystemSynchronizedBuildFileExceptionSet section */
```

（`SortingGameApp.swift` 含 `@main`，排除后 iOS 用自己的入口，避免双重入口冲突。）

- [x] **Step 4: 更新 iOS App 入口**

重写 `SortingGame-iOS/SortingGame_iOSApp.swift`：

```swift
import SwiftUI

@main
struct SortingGame_iOSApp: App {
    var body: some Scene {
        WindowGroup {
            MainMenuView()
        }
    }
}
```

- [x] **Step 5: 验证两端编译**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' build`
预期：BUILD SUCCEEDED

Run: `xcodebuild -scheme SortingGame-iOS -destination 'generic/platform=iOS Simulator' build`
预期：BUILD SUCCEEDED

若 iOS 编译报 `UITouch` 找不到，确认 `GameScene.swift` 顶部有：

```swift
#if canImport(UIKit)
import UIKit
#endif
```

- [x] **Step 6: 提交**

```bash
git add SortingGame.xcodeproj/project.pbxproj SortingGame-iOS/SortingGame_iOSApp.swift
git add -u SortingGame/ SortingGame-iOS/
git commit -m "feat: iOS target 接入共享游戏代码，删除未使用模板文件"
```

---

## Task 14: 三端验证与文档回写

- [x] **Step 1: 全部单元测试**

Run: `xcodebuild -scheme SortingGame -destination 'platform=macOS' test`
预期：所有测试通过（GameCore、GameViewModel、SettingsService、LeaderboardService）
备注：实际验证使用 `-only-testing:SortingGameTests`（模板 SortingGameUITests 存在与本工作无关的既有环境性超时失败）

- [x] **Step 2: iOS 模拟器真机跑一遍**

先查可用模拟器：`xcrun simctl list devices available | grep iPhone`

Run: `xcodebuild -scheme SortingGame-iOS -destination 'platform=iOS Simulator,name=<可用机型>' build`
预期：BUILD SUCCEEDED

（可选人工步骤：在模拟器安装运行，验证触摸点击交换、标记模式、结算流程。）

- [ ] **Step 3: macOS 手动测试清单（在 Xcode 中运行）**

- 主菜单四个按钮全部可用（开始游戏/排行榜/设置/关于）
- 4-7 瓶各难度可开局，开局排列为自然序（非答案）
- 鼠标点击交换有动画 + 音效；选中黄框正确
- 键盘 1-7 选择/交换/放下，Enter 提交，与鼠标操作互不冲突
- 标记模式 ✓ 可切换，交换后标记留在原位（按位置标记）
- 提交反馈"对了 x 个"正确（可对照公布答案验证）
- 公布答案 → 结算显示答案对比（绿/红圈正确）→ 不入排行榜
- 通关 → 庆祝动画 → 结算显示尝试次数与排名 → 入排行榜
- 排行榜 4/5/6/7 分榜切换正确，Top 10 排序正确
- 设置：关音效后无声；色盲模式改变符号大小；昵称保存并在排行榜显示
- 再来一局/更换难度/返回主菜单三个出口行为正确

- [x] **Step 4: 文档回写 + 提交**

更新 `docs/specs/2026-09-20-核心玩法-design.md` 第 10 节里程碑，勾选已完成项（Phase 1-3 全部、Phase 4 的音效/动画/三端项；BGM 与 App Store 提交保持未勾选）。

```bash
git add docs/specs/2026-09-20-核心玩法-design.md docs/superpowers/plans/
git commit -m "docs: 回写 Phase 2-4 完成状态"
```

---

## Task 15: App Store 提交准备（人工清单）

以下为用户手动操作项，代理仅整理清单供执行时核对，不代做：

- [ ] App 图标：在 `Assets.xcassets/AppIcon` 提供 1024×1024 图标（macOS 与 iOS 共用）
- [ ] BGM：用户提供音乐文件后接入 `musicEnabled` 开关（后续小任务）
- [ ] App Store Connect：创建 App 记录、填写名称/描述/关键词/隐私政策 URL
- [ ] 截图：macOS（1280×800 以上）与 iPhone/iPad 各尺寸截图
- [ ] 版本号与签名：确认 Bundle ID、Team、证书（Release 配置）
- [ ] 年龄分级问卷、出口合规（无加密豁免声明）

---

## 自查记录（Self-Review）

- **规格覆盖**：PRD Phase 2（键盘✓ 标记✓ 公布答案✓ 设置✓）、Phase 3（存储✓ 界面✓ 结算集成✓）、Phase 4（音效✓ 动画✓ 三端✓ App Store 清单✓；BGM 因缺资产明确列为后续）
- **类型一致性**：`engine.tap(position:)` / `BottleNode.setMarked(_:)` / `SettingsService.shared`（@MainActor）/ `LeaderboardService.addEntry(bottleCount:attempts:playerName:) -> Int?` 在各 Task 间已对齐
- **测试确定性**：修复了旧 `testSubmitIncorrectAnswer` 的随机性隐患；`testEnterSubmits` 不断言具体"对了 x 个"数值
